# PawPocket runtime assets

This directory is the runtime destination for the first child-home slice.

Expected WebP files and SHA-256 values are listed in `asset-manifest.json`.
Lossless PNG production sources remain in the reviewed `PawPocket_Production_Assets_Phase1.zip` handoff package; the web repository uses WebP at runtime.
